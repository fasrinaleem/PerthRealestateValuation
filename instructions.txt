1. Download All Packages from requirements.txt file
        pip3 install -r requirements.txt
2. Train the model
 you can either tra the model or run app then train the model from gui
        python3 train_model.py

3. Run the Application (It takes sometime to load the frontend for firstime)
        streamlit run app/main.py

